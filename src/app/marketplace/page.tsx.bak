"use client";

import React from 'react';
import FluidLayout from '@/components/layout/FluidLayout';
import dynamic from 'next/dynamic';

const FluidMarketplace = dynamic(() => import('@/components/sections/FluidMarketplace'), {
  ssr: false,
  loading: () => <div className="min-h-screen bg-gradient-to-br from-slate-900 to-blue-900/20 animate-pulse rounded-2xl" />
});

export default function MarketplacePage() {
  return (
    <FluidLayout>
      <FluidMarketplace />
    </FluidLayout>
  );
}